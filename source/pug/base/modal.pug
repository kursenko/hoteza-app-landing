mixin modal(mods)
  .modal.modal--preload(class=mods)&attributes(attributes)
    .modal__wrapper
      .modal__overlay
      .modal__content
        if block
          block
        button.modal__close-btn(type="button", aria-label="Закрыть попап", data-close-modal)
          +icon("icon-close1", 16, 16)
